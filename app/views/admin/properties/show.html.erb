<div class="max-w-6xl mx-auto px-4 py-8">
  <!-- Navegação Admin -->
  <div class="mb-8 flex justify-between items-center">
    <div class="space-x-4">
      <%= link_to "← Voltar aos imóveis admin", admin_properties_path, 
          class: "text-purple-600 hover:text-purple-800 font-medium" %>
      <%= link_to "Ver no site público", property_path(@property), 
          class: "text-blue-600 hover:text-blue-800 font-medium", target: "_blank" %>
    </div>
    
    <div class="space-x-2">
      <%= link_to "Editar", edit_admin_property_path(@property), 
          class: "bg-yellow-500 hover:bg-yellow-600 text-white px-4 py-2 rounded transition-colors" %>
      <%= button_to "Deletar", admin_property_path(@property), 
          method: :delete,
          data: { confirm: "Tem certeza que deseja deletar este imóvel?" },
          class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded transition-colors" %>
      <%= button_to "Logout", destroy_user_session_path, 
          method: :delete,
          class: "bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded transition-colors" %>
    </div>
  </div>

  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <!-- Header com badge admin -->
    <div class="p-8 border-b bg-purple-50">
      <div class="flex items-start justify-between">
        <div>
          <div class="flex items-center gap-2 mb-2">
            <span class="bg-purple-100 text-purple-800 text-xs font-semibold px-2.5 py-0.5 rounded">ADMIN VIEW</span>
          </div>
          <h1 class="text-3xl font-bold text-gray-900 mb-2"><%= @property.title %></h1>
          <p class="text-xl text-blue-600 mb-1">📍 <%= @property.neighborhood.name %></p>
          <p class="text-3xl font-bold text-green-600">R$ <%= number_with_delimiter(@property.price, delimiter: '.') %></p>
        </div>
      </div>
    </div>

    <!-- Resto igual à view pública -->
    <!-- Galeria de Imagens -->
    <% if @property.images.any? %>
      <div class="col-span-full">
        <label class="block text-sm/6 font-medium text-gray-900 mb-4">Imagens Atuais</label>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4" id="current-images">
          <% @property.images.each do |image| %>
            <div class="relative group" id="<%= dom_id(image, :container) %>">
              <div class="aspect-w-1 aspect-h-1 w-full overflow-hidden rounded-lg bg-gray-200">
                <%= image_tag image, class: "h-full w-full object-cover object-center group-hover:opacity-75" %>
              </div>
              
              <!-- BOTÃO DE REMOÇÃO CORRIGIDO -->
              <%= button_to admin_property_image_path(@property, image), 
                  method: :delete,
                  data: { 
                    "turbo-confirm": "Tem certeza que deseja remover esta imagem?",
                    "turbo-method": "delete"
                  },
                  class: "absolute -top-2 -right-2 rounded-full bg-red-600 p-1 text-white shadow-sm hover:bg-red-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-600",
                  form: { style: "display: inline;" } do %>
                <svg class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <!-- Descrição -->
    <div class="p-8">
      <h2 class="text-2xl font-bold mb-4">Descrição</h2>
      <div class="prose max-w-none">
        <%= simple_format(@property.description) %>
      </div>
    </div>

    <!-- Informações do Bairro -->
    <div class="p-8 bg-gray-50">
      <h2 class="text-2xl font-bold mb-4">Sobre o Bairro</h2>
      <p class="text-lg text-gray-700 mb-4"><%= @property.neighborhood.name %></p>
      <%= link_to "Gerenciar bairros", admin_neighborhoods_path, 
          class: "text-purple-600 hover:text-purple-800 font-medium" %>
    </div>
  </div>
</div>

<!-- Modal para ampliar imagens (mesmo da view pública) -->
<div id="imageModal" class="fixed inset-0 bg-black bg-opacity-75 hidden z-50 flex items-center justify-center p-4">
  <div class="relative max-w-4xl max-h-full">
    <img id="modalImage" src="" alt="" class="max-w-full max-h-full object-contain">
    <button onclick="closeModal()" class="absolute top-4 right-4 text-white text-2xl hover:text-gray-300">✕</button>
  </div>
</div>

<script>
function openModal(imageSrc) {
  document.getElementById('modalImage').src = imageSrc;
  document.getElementById('imageModal').classList.remove('hidden');
}

function closeModal() {
  document.getElementById('imageModal').classList.add('hidden');
}

document.getElementById('imageModal').addEventListener('click', function(e) {
  if (e.target === this) closeModal();
});
</script>