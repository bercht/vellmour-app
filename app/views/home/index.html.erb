<div class="max-w-6xl mx-auto px-4 py-8">
  <!-- Hero Section -->
  <div class="text-center mb-16">
    <h1 class="text-5xl font-bold text-gray-900 mb-6">Encontre Seu Lar Ideal</h1>
    <p class="text-xl text-gray-600 mb-8">Os melhores imóveis da cidade estão aqui</p>
    <%= link_to "Ver Todos os Imóveis", properties_path, 
        class: "bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-lg text-lg font-semibold transition-colors inline-block" %>
  </div>

  <!-- Imóveis em Destaque -->
  <div class="mb-16">
    <div class="flex justify-between items-center mb-8">
      <h2 class="text-3xl font-bold text-gray-900">Imóveis em Destaque</h2>
      <%= link_to "Ver Todos →", properties_path, 
          class: "text-blue-600 hover:text-blue-800 font-medium" %>
    </div>

    <% if @featured_properties.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <% @featured_properties.each do |property| %>
          <div class="bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow">
            <!-- Imagem -->
            <div class="h-64 bg-gray-200 relative">
              <% if property.images.any? %>
                <%= image_tag property.images.first, 
                    class: "w-full h-full object-cover" %>
              <% end %>
              <div class="absolute top-4 right-4 bg-green-500 text-white px-3 py-1 rounded-full font-bold">
                R$ <%= number_with_delimiter(property.price, delimiter: '.') %>
              </div>
              <div class="absolute top-4 left-4 bg-yellow-500 text-white px-3 py-1 rounded-full font-bold text-sm">
                ⭐ DESTAQUE
              </div>
            </div>
            
            <!-- Conteúdo -->
            <div class="p-6">
              <h3 class="text-xl font-bold text-gray-900 mb-2"><%= property.title %></h3>
              <p class="text-blue-600 mb-3">📍 <%= property.neighborhood.name %></p>
              <p class="text-gray-600 mb-4 line-clamp-3"><%= truncate(property.description, length: 120) %></p>
              
              <%= link_to "Ver Detalhes", property_path(property), 
                  class: "w-full bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-4 rounded transition-colors block text-center" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-12 bg-gray-100 rounded-lg">
        <p class="text-xl text-gray-500">Imóveis em destaque serão exibidos aqui em breve!</p>
        <% if user_signed_in? %>
          <%= link_to "Adicionar Imóveis", admin_properties_path, 
              class: "mt-4 inline-block bg-purple-500 hover:bg-purple-600 text-white px-6 py-2 rounded transition-colors" %>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Seção Bairros -->
  <div class="mb-16">
    <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">Explore por Bairro</h2>
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
      <% @neighborhoods.each do |neighborhood| %>
        <%= link_to neighborhood_path(neighborhood), 
            class: "bg-white rounded-lg shadow hover:shadow-lg transition-shadow p-4 text-center group" do %>
          <div class="text-2xl mb-2">🏘️</div>
          <h3 class="font-semibold text-gray-900 group-hover:text-blue-600 transition-colors">
            <%= neighborhood.name %>
          </h3>
          <p class="text-sm text-gray-500">
            <%= pluralize(neighborhood.properties.count, 'imóvel', 'imóveis') %>
          </p>
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- Call to Action -->
  <div class="bg-blue-600 rounded-lg p-12 text-center text-white">
    <h2 class="text-3xl font-bold mb-4">Não encontrou o que procura?</h2>
    <p class="text-xl mb-8">Entre em contato conosco e encontraremos o imóvel ideal para você!</p>
    <div class="flex justify-center space-x-4">
      <button class="bg-green-500 hover:bg-green-600 text-white px-8 py-3 rounded-lg font-bold transition-colors">
        📱 WhatsApp
      </button>
      <button class="bg-white hover:bg-gray-100 text-blue-600 px-8 py-3 rounded-lg font-bold transition-colors">
        📧 Email
      </button>
    </div>
  </div>
</div>