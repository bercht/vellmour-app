<!DOCTYPE html>
<html>
  <head>
    <title>Vellmour Imóveis</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <!-- Spotlight.js CSS -->
    <link rel="stylesheet" href="https://rawcdn.githack.com/nextapps-de/spotlight/0.7.8/dist/css/spotlight.min.css">
    
    <%= javascript_importmap_tags %>
    
    <!-- Spotlight.js JavaScript -->
    <script src="https://rawcdn.githack.com/nextapps-de/spotlight/0.7.8/dist/js/spotlight.min.js"></script>
    
  </head>

  <body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Navbar (renderizada condicionalmente - exceto home/index) -->
    <% unless controller_name == "home" && action_name == "index" %>
      <%= render 'shared/navbar' %>
    <% end %>
    <!-- Flash messages -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 mb-4 relative z-50">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 mb-4 relative z-50">
        <%= alert %>
      </div>
    <% end %>

    <!-- Main content -->
    <main class="flex-grow">
      <%= yield %>
    </main>

    <!-- Footer (apenas em páginas públicas) -->
    <% unless controller_name == "admin" || params[:controller]&.starts_with?("admin/") %>
      <%= render 'shared/footer' %>
    <% end %>
  </body>
</html>